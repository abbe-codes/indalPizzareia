{
  "name": "indal-pizzeria",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "En svensk menysida för Indal Pizzeria.",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "start": "node scripts/serve.mjs",
    "check": "node --check script.js && node --check scripts/serve.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=24"
  }
}
